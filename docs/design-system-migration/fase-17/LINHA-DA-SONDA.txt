(()=>{const a=document.documentElement.getAttribute('data-app');if(a!=='chamadoshs')return'ERRO: pagina nao e do chamadoshs, e sim '+(a||'(sem data-app)');const W=innerWidth,H=innerHeight,T=1,A=40,R=.25;const v=e=>{const s=getComputedStyle(e);if(s.display==='none'||s.visibility==='hidden'||s.opacity==='0')return!1;const r=e.getBoundingClientRect();return r.width>0&&r.height>0};const n=e=>{const c=(e.getAttribute('class')||'').trim().split(/\s+/).filter(Boolean).slice(0,2).join('.'),t=(e.textContent||'').trim().slice(0,22);return e.tagName.toLowerCase()+(c?'.'+c:'')+(t?' "'+t+'"':'')};const D=[...document.body.querySelectorAll('*')].filter(v);const rc=e=>{for(let p=e.parentElement;p&&p!==document.body;p=p.parentElement){const o=getComputedStyle(p);if(/(auto|scroll|hidden|clip)/.test(o.overflowX+' '+o.overflow))return!0}return!1};const ct=D.filter(e=>{const r=e.getBoundingClientRect();return(r.right>W+T||r.left<-T)&&!rc(e)}).map(e=>{const r=e.getBoundingClientRect();return n(e)+' ['+Math.round(r.left)+'..'+Math.round(r.right)+'] de '+W});const I='button, a[href], input, select, textarea, [role="button"], [role="tab"], [role="switch"], [tabindex]:not([tabindex="-1"])';const C=[...document.querySelectorAll(I)].filter(v).map(e=>({e,r:e.getBoundingClientRect()}));const so=C.filter(({r})=>r.width<=1&&r.height<=1);const pq=C.filter(({r})=>r.width>1&&r.height>1&&Math.min(r.width,r.height)<A).map(({e,r})=>n(e)+' '+Math.round(r.width)+'x'+Math.round(r.height));const af=C.filter(({r})=>r.top>=0&&r.left>=0&&r.bottom<=H&&r.right<=W);const cb=af.filter(({e,r})=>{const x=document.elementFromPoint(r.left+r.width/2,r.top+r.height/2);return x&&x!==e&&!e.contains(x)&&!x.contains(e)}).map(({e,r})=>{const x=document.elementFromPoint(r.left+r.width/2,r.top+r.height/2);return n(e)+' coberto por '+n(x)});const F=D.filter(e=>!e.children.length&&(e.textContent||'').trim()&&getComputedStyle(e).pointerEvents!=='none');const sb=[];for(let i=0;i<F.length;i++)for(let j=i+1;j<F.length;j++){const x=F[i],y=F[j];if(x.contains(y)||y.contains(x))continue;const p=x.getBoundingClientRect(),q=y.getBoundingClientRect();const w=Math.min(p.right,q.right)-Math.max(p.left,q.left),h=Math.min(p.bottom,q.bottom)-Math.max(p.top,q.top);if(w<=0||h<=0)continue;const m=Math.min(p.width*p.height,q.width*q.height);if(m>0&&w*h/m>=R)sb.push(n(x)+' sobre '+n(y))}return JSON.stringify({regua:W+'x'+H,rota:location.pathname,cortado:ct.length,cortado_quais:ct.slice(0,6),alvo:pq.length,alvo_quais:pq.slice(0,14),sr_only_ignorados:so.length,coberto_resumo:cb.length+' em '+af.length+' aferidos, de '+C.length+' controles',coberto:cb.length,coberto_quais:cb.slice(0,6),sobreposto:sb.length,sobreposto_quais:sb.slice(0,6),controles:C.length,cobertura_aferida_em:af.length+' de '+C.length},null,1)})()
