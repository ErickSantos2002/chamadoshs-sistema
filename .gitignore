# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build outputs
build
coverage

# TypeScript
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Backups temporários de edição
*.bak
.*.bak

# ── Capturas de evidência ────────────────────────────────────────────
#
# As dezesseis do Checkpoint 3 foram feitas contra PRODUÇÃO, em leitura pura,
# por decisão do operador em 04/09/2026 — não há Docker nesta máquina e a
# credencial de superusuário do PostgreSQL 18 é desconhecida, então não houve
# ambiente local para levantar massa de teste.
#
# Por isso elas contêm nome de solicitante, título de chamado e protocolo de
# gente real, e NÃO entram no repositório. O diretório inteiro fica de fora.
#
# As fichas da §29 e este protocolo são texto, sem dado pessoal, e vão
# normalmente.
docs/design-system-migration/capturas-locais/
